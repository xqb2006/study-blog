# 个人博客项目设定（暂定）

> 状态：方案讨论阶段，尚未开始正式开发。
>
> 最后整理：2026-08-17

## 一、项目目标

制作一个长期运行、低维护成本的个人博客网站。

核心目标：

- 以文章阅读和个人知识沉淀为主
- 不依赖自建服务器和数据库
- 尽量减少第三方服务
- 内容可导出、可迁移、可长期保存
- 桌面端、平板端、移动端保持一致体验
- 首页可以有仪表盘感，但文章详情页优先保证阅读舒适度

## 二、暂定技术方向

推荐方案：

- 框架：Astro
- 内容：Markdown，必要时支持 MDX
- 代码仓库：GitHub
- 部署：Cloudflare Pages
- 数据库：不使用
- 后端服务：不使用
- 登录系统：第一阶段不使用
- 评论系统：第一阶段不使用
- 自建服务器：不使用

文章发布流程暂定为：

```text
编写 Markdown 文章 → 提交到 GitHub → 自动构建 → 静态网站发布
```

## 三、视觉与交互方向

参考方向：Dashboard Layout（仪表盘布局）。

可以吸收的视觉元素：

- 深色侧边导航栏
- 顶部工具栏
- 搜索入口
- 个人信息区域
- 卡片式内容面板
- 分类、标签和数据概览
- 紧凑但清晰的网格布局
- 移动端侧边栏抽屉

需要控制的方向：

- 首页具有仪表盘识别度，但不能像传统后台一样冰冷
- 文章详情页不能过度面板化，应以长文阅读为核心
- 数据展示服务于内容，不为装饰增加无意义指标
- 保持统一的间距、颜色、边框、圆角和交互反馈
- 优先可读性、可维护性和无障碍体验

## 四、第一阶段功能边界

第一版计划包含：

- 首页
- 文章详情页
- 文章归档页
- 分类页
- 标签页
- 关于页面
- 404 页面
- RSS / Atom 订阅
- 网站地图
- 基础 SEO 信息
- 响应式布局
- 空状态和加载状态

第一版暂不包含：

- 用户注册和登录
- 数据库
- 自建后台
- 在线 Markdown 编辑器
- 评论系统
- 点赞和收藏
- 私信功能
- 服务器监控
- Live2D 助手
- AI 聊天
- 复杂的动态接口

## 五、从 NavHub 项目中保留的参考价值

参考项目：`ldt471146/navhub`

值得借鉴：

- 个人导航/工作台式的信息架构
- 侧边栏 + 顶部工具栏的布局方式
- 分类、标签、搜索组合
- 卡片化内容展示
- 个人化首页和工具区
- 桌面端与移动端的响应式处理
- 浅色/深色主题切换的产品思路

不直接继承：

- FastAPI 后端
- SQLite 数据库
- systemd 服务
- Nginx 反向代理
- 邮箱验证码
- 双密码权限
- 服务器监控
- AI API 作为运行时依赖
- 多个外部服务组成的运行链路

结论：

> 借鉴 NavHub 的仪表盘式体验和交互思路，不继承它的后端复杂度。

## 六、低维护原则

- 优先静态生成
- 不为小功能引入后端
- 不引入没有明确价值的依赖
- 不把文章锁定在某个平台的专有格式中
- 所有文章和配置都应能通过 Git 版本管理
- 图片和资源要有清晰的备份与迁移方案
- 第三方服务不是必需品时，不作为核心链路
- 每项新功能都要评估长期维护成本
- AI 修改代码前必须先阅读项目说明和现有结构
- AI 不得擅自更换技术栈、删除文件或扩展功能范围

## 七、暂存的待确认事项

- 是否使用自定义域名
- 是否需要网页后台写文章
- 是否接受使用 Markdown
- 博客主要内容主题
- 是否需要深色模式
- 是否需要站内搜索
- 是否需要访问统计
- 是否需要独立的作品集/项目展示区
- 是否需要将首页做成完整仪表盘
- 品牌名称、Logo 和配色

## 八、开发协作要求（暂定）

每次开发任务遵循以下流程：

1. 先阅读项目文档和相关代码
2. 先说明实现方案，再修改文件
3. 一次只完成一个明确功能
4. 不主动增加未要求的功能
5. 修改后执行针对性的构建或检查
6. 汇报修改的文件、验证结果和遗留问题

## 九、astro-koharu 参考结论

参考项目：`ldt471146/astro-koharu`

这个项目比 NavHub 更接近本博客的技术方向。它是一个基于 Astro 的博客主题，并包含独立的 CMS；公开博客仍然可以静态生成，CMS 则负责内容编辑、媒体管理和构建发布。

值得借鉴：

- Astro 静态输出作为公开博客基础
- Markdown 文件作为主要内容源
- 内容集合与 Schema 校验
- 分类、标签、系列文章和自定义页面
- Pagefind 无后端站内搜索
- RSS、站点地图和基础 SEO 体系
- 深色/浅色主题切换
- 图片 LQIP 占位和性能优化思路
- Markdown 增强能力按功能独立开关
- 将 CMS 与公开博客分成不同层级
- 用文档记录架构决策、内容系统和部署方式

暂时不直接引入：

- 完整 CMS
- BlockNote 富文本编辑器
- 评论系统
- 实时 Bangumi 页面
- Mermaid、信息图、练习题等大量增强语法
- Docker、Nginx 和 Node 服务端部署
- 过多 React/CMS 依赖

原因：这些功能本身有价值，但会增加依赖、构建时间、调试范围和后期维护成本。第一阶段优先采用 Astro 的静态博客核心，之后根据真实写作需求逐项加入。

暂定技术路线更新为：

```text
Astro 静态博客核心
    + Markdown / MDX 内容
    + Content Collections / Schema
    + Pagefind 搜索
    + RSS / Sitemap / SEO
    + 仪表盘式首页
    + 可选的独立 CMS（后续评估）
```

CMS 方向暂定：

> 第一阶段不开发完整 CMS；先保证 Markdown 写作流程稳定。如果后续确实需要网页编辑，再单独设计一个不影响公开博客静态部署的内容管理层。

## 十、astro-koharu 的部署方式判断

该项目没有固定绑定某一个平台，而是提供多种部署方式：

- 公开博客：可以部署到 Vercel、Netlify 等自动构建平台
- 自托管博客：可以通过 Docker 构建静态文件，再由 Nginx 提供访问
- CMS：是独立的 Node/Hono 应用，不适合直接部署到只提供静态托管的平台
- CMS 与公开博客：可以分开部署，CMS 修改内容后触发博客重新构建

因此需要区分两件事：

1. **网站部署方式**：博客最终放在哪里运行
2. **内容上传方式**：文章和图片通过 Git、CMS 或其他方式写入项目

目前仓库更接近以下模式：

```text
CMS / 本地编辑器
        ↓
写入 Markdown 和资源文件
        ↓
触发 Astro 构建
        ↓
Vercel / Netlify / Docker 发布公开博客
```

如果目标是低维护，优先考虑：

- 公开博客部署到 Vercel 或 Netlify
- 文章继续使用 Markdown
- 暂不部署完整 CMS
- 后续如果需要网页上传，再单独部署 CMS 或改成 Git 提交式上传

如果目标是网页端直接写文章，则需要接受 CMS 的运行成本；仅把博客部署到 Vercel 或 Netlify，不能单独承载这个项目里的完整 CMS。

## 十一、网页写作与零服务器维护的折中方案

可采用 Git 型网页 CMS，而不是部署完整 CMS 服务器：

```text
网页 CMS
   ↓ 通过 GitHub 授权提交
GitHub 仓库中的 Markdown / 图片
   ↓ 自动触发构建
Vercel / Netlify / Cloudflare Pages
   ↓
公开博客
```

候选方案：

- 优先评估 Pages CMS：无独立数据库和后端，内容直接存放在 GitHub 仓库
- 备选 Keystatic GitHub Mode：可将编辑界面集成进现有项目，但需要配置 GitHub 模式
- 暂不优先使用完整的自托管 CMS：会重新引入服务器和应用维护

这个方案可以同时满足：

- 在浏览器中写文章
- 浏览器上传图片
- 文章仍以 Markdown 保存
- GitHub 保留版本历史和备份
- 网站继续使用静态部署
- 不需要维护数据库、Linux 服务或 CMS 服务器

需要接受的限制：

- 需要使用 GitHub 账号授权
- 保存文章本质上是创建 Git 提交，网站通常需要等待自动构建后更新
- 网页 CMS 是第三方服务，未来可能发生服务或功能变化
- 不能完全等同于 WordPress 的实时数据库后台

## 十二、零预算约束

目标：网站长期运行费用为 0 元。

暂定采用：

- 博客托管：Cloudflare Pages 免费计划
- 默认地址：使用平台提供的免费子域名
- 内容仓库：GitHub 免费仓库
- 网页编辑：Git 型网页 CMS，或直接使用 GitHub 网页编辑作为备用方案
- 图片：优先压缩后随仓库保存
- AI 功能：不作为网站运行时依赖，不接入需要付费的 AI API
- 评论、统计、音乐和外部动态服务：第一阶段不启用
- 自定义域名：暂不购买

零预算的实际边界：

- 可以免费拥有和运行博客，但不能免费拥有自己的顶级域名
- 网站地址将使用平台子域名
- 免费平台有构建次数、文件数量或流量限制，个人博客通常应足够
- 第三方网页 CMS 如果临时不可用，必须能够退回 GitHub 网页编辑
- 不能把核心内容只保存在第三方 CMS 中

暂不优先选择 Vercel 的原因：其 Hobby 免费计划面向个人、非商业用途；当前项目是纯静态博客，Cloudflare Pages 的免费静态资源托管和 Git 自动部署更符合零预算约束。

## 十三、长期失效风险与迁移原则

截至 2026-08-17，这套方案不能承诺任何第三方服务永久免费或永久存在，但可以把迁移成本控制得很低。

主要风险：

- Cloudflare Pages 免费额度或规则未来变化
- Git 型网页 CMS 停止服务或改变授权方式
- Astro、Node 或主题依赖升级后出现构建兼容问题
- GitHub 账号、仓库或权限配置出现问题

稳定性措施：

- 文章只保存为 Markdown，不使用 CMS 专有数据库格式
- 图片和站点配置保存在仓库中
- 锁定依赖版本，不自动升级生产依赖
- 保留可在本地执行的构建命令
- 定期下载整个 Git 仓库作为离线备份
- 保留 GitHub 网页编辑作为 CMS 故障时的备用发布方式
- 不把核心功能依赖在运行时 API 上
- 不使用 Astro SSR、数据库、登录和动态接口

迁移目标：

> 如果 Cloudflare Pages 或网页 CMS 未来失效，只需要把同一个 GitHub 仓库切换到其他静态托管平台，或本地构建 `dist/` 后上传到新的静态主机，不需要重写文章和网站主体。

## 十四、开源发布策略

目标：将项目做成其他用户可以 Fork、配置、部署的博客模板，而不是公开个人隐私和个人文章。

开源产品边界：

- 公开：主题代码、组件、配置示例、示例文章、部署文档
- 不公开：个人文章、个人图片、账号信息、密钥、统计 ID、私有域名配置
- 配置项：站点名称、头像、简介、社交链接、导航菜单、评论和统计开关
- 部署方式：优先支持 GitHub Fork + Cloudflare Pages

许可证注意事项：

- `astro-koharu` 当前使用 AGPL-3.0，应保留上游版权和许可证说明
- 基于其代码修改和再发布时，不能直接改成 MIT 或闭源授权
- 需要在 README 中注明上游项目、修改内容和本项目许可证
- 所有新增依赖、字体、图片和插画都要单独确认授权

推荐仓库体验：

```text
Fork 仓库
   ↓
修改 config/site.yaml 和环境示例
   ↓
替换头像、Logo、示例文章
   ↓
连接 Cloudflare Pages
   ↓
完成部署
```

开源第一版应优先提供：

- 清晰的 README
- 5 分钟部署教程
- 配置项说明
- 示例配置文件
- 示例内容与内容 Schema
- 常见故障排查
- LICENSE 和第三方声明
- 构建检查和基础测试
- 截图与在线演示

暂不承诺：

- 为每个用户提供托管服务
- 免费提供自定义域名
- 免费提供 AI API、评论、统计或图片存储
- 兼容所有部署平台
- 维护用户自行修改后的版本
